#include <iostream>
#include <stdio.h>
#include <string.h>
using namespace std;

int main()
{
	char str1[102],str2[102],res[104];
	int len1,len2,len3;
	scanf("%s%s",str1,str2);
	
	len1=strlen(str1);
	len2=strlen(str2);

	if(len1 > len2)
	{
		int pos1,pos2;
		pos1=len1-1;
		pos2=len2-1;
	}
	else if(len2 > len1)
	{
	
	}
	else if(len1==len2)
	{
		
	}
	
	return 0;
}

